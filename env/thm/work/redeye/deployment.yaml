apiVersion: apps/v1
kind: Deployment
metadata:
  name: redeye
spec:
  template:
    spec:
      volumes:
        - name: redeye-files
          hostPath:
            path: /disk1/redeye/files-thm
        - name: redeye-db
          hostPath:
            path: /disk1/redeye/db-thm
      containers:
        - name: reddeye
          env:
            - name: NEO4J_DATABASE
              value: RedEyeWorkTHM
